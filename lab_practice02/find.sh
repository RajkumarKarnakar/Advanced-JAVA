#!/bin/bash
# This script finds files with the name abc.txt in the current directory or all its subdirectories

# Use the find command with a wildcard and the -name option to search for files by exact name
find . -name file1.txt
