You can save this script as add_and_change.sh and make it executable with chmod +x add_and_change.sh. Then you can run it with ./add_and_change.sh and see the output.

The output of the ls -l command will look something like this: